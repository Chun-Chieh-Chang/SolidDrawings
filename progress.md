## 2026-06-26 — Task 2: Annotations / GD&T (P1)

**Execution mode:** Manual in-session (annotation types + store + SVG render + UI)
**Result:** ✅ Complete
- Defined `DatumFeature`, `GeometricTolerance`, `DrawingAnnotation` types in types.ts
- Added annotation CRUD actions (addAnnotation, updateAnnotationPosition, removeAnnotation) to drawing-state.ts
- Rewrote AnnotationLayer.tsx: datum triangle + label box, GD&T feature control frame (multi-cell SVG), DnDGroup drag support
- Added Datum and GD&T buttons in DrawingSheet header, wired to store actions
- tsc --noEmit: clean | npx jest: 80 passed

**Pending:** Tasks 3-6

---

## 2026-06-26 — Task 5: Frontend Test Infrastructure Fix (Quick Win)

**Diagnosis:** No vitest/playwright conflict existed — project uses Jest (not vitest). Jest test dir (`**/__tests__/**`) is different from Playwright dir (`tests/e2e/`). Missing: `test:frontend` script, component render tests, and gap report had incorrect info.

**Done:**
- Installed `@testing-library/react`, `@testing-library/jest-dom`, `jest-environment-jsdom`
- Added `test:frontend` (jest) + `test` (jest + pytest) scripts to package.json
- Wrote SheetFormatSelector smoke test: 4 tests (render, dropdown, highlight, onChange)
- Total frontend tests: 80 passed (5 suites)
- Updated gap-report.md: Testing 30%→35%, removed incorrect vitest/playwright risk
- `tsc --noEmit`: clean | `npx jest`: 80 passed

**Next:** Task 2 (Annotations/GD&T P1) — start with type definitions + store slices

---

## 2026-06-25 (later) — Executed Task 1: Unfold/Fold

**Execution mode:** Manual (sub-agent dispatch failed — see RCA/CAPA in DEV_LOG.md)
**Result:** ✅ Complete
- Backend generate_unfold/generate_fold already existed (lines 5847-5884)
- API routes already existed (`/unfold`, `/fold`)
- Added HeavyEngineClient.createUnfold/createFold
- Added sheet-metal-builders handleUnfold/handleFold
- Wired RibbonController buttons (was stubs -> dedicated handlers)
- tsc --noEmit: clean | pytest: 100 passed

**Pending:** Tasks 2-6 (start with Task 2: Annotations/GD&T next)

---

## 2026-06-25 — Sprint Plan: P1 Gap Closure + P2 Foundation

Generated by gap-planner from gap-analysis-report.md.

**Sprint scope:** 6 tasks targeting Sheet Metal, Drawing, Sketch, Testing
**Target score improvements:**
- Sheet Metal: 85% → 90%
- Drawing: 50% → 60%
- Sketch: 85% → 88%
- Testing: 30% → 35%

**Tasks:**
1. Unfold / Fold (P1)
2. Annotations / GD&T (P1)
3. Sketch Fillet / Chamfer (P2)
4. Crop View / Auxiliary View (P2)
5. 前端測試基礎設施修復 (P2)
6. Bend Table (P2)

---

## Session: 2026-06-25 (Detail View Frontend)
- **Detail View (局部放大圖) Frontend** — Complete implementation in DrawingSheet.tsx:
  - Added `DETAIL` to all type unions: `types.ts`, `drawing-state.ts` (interface + implementation), `store/index.ts`
  - `DrawingView`: Added detail tool state (showDetailTool, detailCenter, detailRadius, isDrawingDetail), `handleSvgClick` detail mode, `handleCreateDetailView` handler
  - `DrawingView` header: Detail tool toggle button (concentric circles icon, emerald theme)
  - `DrawingView` overlay: Detail tool UI with instructions + Create Detail / Cancel buttons
  - SVG rendering: detailBounds circle (dashed emerald circle with DETAIL label) on parent views, preview circle while drawing
  - `DrawingSheet` main: detailBounds state, handleDetailCreated callback, props wiring
  - ViewBox zoom: DETAIL views center on detailBounds.cx/cy with radius*1.5 zoom
  - Parent view line inheritance: DETAIL views inherit lines from parent (projection or section data)
  - `getProjPos`: DETAIL returns [x, y] (same as FRONT projection)
  - `tsc --noEmit`: zero new errors
  - Backend tests: 16 passed (no regression)
  - Fixed features.py imports: bare `from geometry_service` → relative `from .geometry_service`
- **Test coverage expansion**:
  - `test_geometry_service_ext.py`: +17 tests for features.py (generate_box, generate_cylinder, generate_sphere, generate_rib, generate_section_view) with HAS_OCC=False patching
  - `test_api_geometry.py`: +4 section_view edge case tests (all plane types, structure validation, polygon points)
  - **Total backend tests**: 55 passed (was 16) — all green
  - Imports fix: features.py now uses relative imports `from .geometry_service` instead of bare imports

## 2026-06-25 — Sprint Plan: Drawing + Sheet Metal + Surfacing

Generated by gap-planner from gap-analysis-report.md.

**Sprint scope:** 6 tasks targeting Drawing (P0), Sheet Metal (P0), Assembly (P0), Surfacing (P1), Part Features (P1)
**Target score improvements:**
- Drawing: 15% → 40%
- Sheet Metal: 70% → 80%
- Surfacing: 30% → 50%
- Assembly: 25% → 40%

**Tasks:**
1. Unfold/Fold (P0) ✅
2. Drawing Standard 3 Views + Model View (P0) ✅
3. Assembly Advanced Mates — PROFILE_CENTER (P0) ✅
4. Boundary Surface + Trim Surface (P1) ✅
5. Rib (P1) ✅
6. Verification + score update ✅

## 2026-06-25 — Sprint 2 Plan: Close Remaining P1 + Tech Debt

Generated by manual gap-planner execution.

**Sprint scope:** 3 tasks targeting Split/Combine (P1), Base Flange Tab (P1), geometry_service.py refactor (tech debt)
**Target score improvements:**
- Sheet Metal: 75% → 90%
- Surfacing: 45% → 50%
- Assembly: 35% → 40%
- **Overall: ~50% → ~60%**

**Tasks:**
1. ✅ Split/Combine boolean operations (P1) — backend (generate_split/generate_combine), both pipeline handlers, frontend buttons (FEATURES tab), types (SPLIT|COMBINE in pendingFeatureCommand), tsc passes clean
2. ✅ Base Flange Tab (P1) — store types (BASE_FLANGE_TAB), SHEET_METALS button (first), rebuild pipeline handlers (x2), tsc passes clean
3. ✅ Refactor geometry_service.py → split into surfacing.py (extracted) + sheet_metal.py (extracted) — geometry_service.py reduced ~270 lines, all syntax verified

## 2026-06-25 — Testing infrastructure setup
- **Backend pytest**: 32 new tests (13 API integration + 19 unit) for SPLIT, COMBINE, BASE_FLANGE_TAB, boundary/trim surface, rebuild pipeline — all pass with HAS_OCC=False patching
- **Frontend Jest**: 8 new smoke tests for feature type unions + parameter shapes — all pass
- **Total test count**: 54 backend + 76 frontend = **130 tests**, all green
- **Coverage**: Testing infra went from 5% → 20% (still needs more, but foundation is solid)
- Overall: 45% → ~55%

**Tasks:**
1. Drawing — Standard 3 Views + Model View Backend API (P0)
2. Drawing — Standard 3 Views Frontend View Rendering (P0)
3. Sheet Metal — Unfold/Fold Feature (P0)
4. Assembly — Advanced Mates (Profile Center, Symmetric, Width) (P0)
5. Surfacing — Boundary Surface + Trim Surface (P1)
6. Part Features — Rib 特徵 Implementation (P1)

## Session: 2026-06-25 (Bend Allowance UI + Forming Tools)
- **Bend Allowance Panel** (`src/ui/SheetMetal/BendAllowancePanel.tsx`):
  - Dedicated PropertyManager-style panel with allowance type selector (K-Factor/Bend Allowance/Bend Deduction)
  - K-factor slider with material presets (Steel/Aluminum/Stainless/Copper/Brass)
  - Bend radius, thickness, angle inputs
  - Relief type (Rectangular/Tear/Obround/None) + Auto Relief + dimensions
  - Live calculation preview showing BA, Setback, Total Flat Length + formula
  - Integrated into SheetMetalPanel when Bend Allowance/K-Factor tools selected
- **Forming Tools** — 5 tool types, full end-to-end pipeline:
  - Backend (`geometry_service.py`): `generate_forming_tool()` dispatches to `_make_louver`, `_make_lance`, `_make_bridge`, `_make_dimple`, `_make_drawn_cutout` — each creates parametric OCCT geometry, cached in `_FORMING_TOOL_SHAPE_CACHE`
  - Backend API (`geometry.py`): `FormingToolRequest` + `POST /forming_tool`
  - Rebuild pipeline: `FORMING_TOOL` case with cache lookup + fallback box + fuse
  - Frontend: `createFormingTool()` in HeavyEngineClient, `handleCreateFormingTool()` in sheet-metal-builders
  - Ribbon: 5 forming tool buttons (Louver, Lance, Bridge, Dimple, Drawn Cutout) in SHEET_METALS tab
  - Props wired through `index.ts` + `page.tsx`
- Ribbon: Bend Allowance button now live (switches to SHEET_METALS tab), BA Calc replaced by forming tools
- `tsc --noEmit`: zero new errors (only pre-existing playwright/jest)
- Python syntax check: geometry_service.py + router both compile clean

## Session: 2026-06-24 (Cleanup + MEC Consolidation + Git Baseline)
- MECE cleanup: removed `diagnose.py`, `diagnose2.py`, `sync.ffs_db`, root `nul`, plugin `nul` files
- Updated `.gitignore`: added `nul`, `vendor/`, `.omo/`, `playwright-report/`, `.opencode/`
- Updated `handover_resume_guide.md` — comprehensive state doc for handoff
- Appended `DEV_LOG.md` with complete Phase 6 entries (Edge Flange, Miter Flange, Hem, Flat Pattern, codebase-memory)
- Updated `progress.md` with current session
- Deleted stale `master` branch (redundant with `main`)
- All changes committed as cleanup baseline
- Pushed to GitHub

## Session: 2026-06-23 (STABLE-3 Closure + Sheet Metal Phase 6)
[Previous session content preserved below]

### Edge Flange
- Real OCCT L-profile sweep geometry via BRepFill_PipeShell
- Full pipeline: Ribbon → handler → HeavyEngineClient → API → geometry_service → cache → rebuild

### Miter Flange
- 2 L-profile segments at 90°, fused
- Same end-to-end pipeline

### Hem
- CLOSED/OPEN/TEARDROP 180° fold types
- GC_MakeArcOfCircle → BRepPrimAPI_MakePrism

### Flat Pattern
- Parametric unfold via K-factor BA formula
- Planar plate on XY plane at z=25

### Codebase-Memory
- MCP server installed and configured (UI on localhost:9749)
